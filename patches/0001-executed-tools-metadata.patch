diff --git a/nanobot/agent/loop.py b/nanobot/agent/loop.py
index 53cb49d..80a9b46 100644
--- a/nanobot/agent/loop.py
+++ b/nanobot/agent/loop.py
@@ -107,6 +107,13 @@ class _LoopHook(AgentHook):
         for tc in context.tool_calls:
             args_str = json.dumps(tc.arguments, ensure_ascii=False)
             logger.info("Tool call: {}({})", tc.name, args_str[:200])
+            # Cozy patch: record executed tool for metadata.executed_tools
+            try:
+                self._loop._last_run_tools.append(
+                    {"name": tc.name, "arguments": tc.arguments}
+                )
+            except Exception:
+                pass
         self._loop._set_tool_context(self._channel, self._chat_id, self._message_id)
 
     async def after_iteration(self, context: AgentHookContext) -> None:
@@ -252,6 +259,10 @@ class AgentLoop:
             self.tools.register(MyTool(loop=self, modify_allowed=_tc.my.allow_set))
         self._runtime_vars: dict[str, Any] = {}
         self._current_iteration: int = 0
+        # Cozy patch: per-request tool execution log; consumers (e.g. the API
+        # server) may clear this before calling process_direct and read it
+        # afterwards to populate response metadata.executed_tools.
+        self._last_run_tools: list[dict[str, Any]] = []
         self.commands = CommandRouter()
         register_builtin_commands(self.commands)
 
diff --git a/nanobot/api/server.py b/nanobot/api/server.py
index ebdee55..d26626d 100644
--- a/nanobot/api/server.py
+++ b/nanobot/api/server.py
@@ -47,7 +47,11 @@ def _error_json(status: int, message: str, err_type: str = "invalid_request_erro
     )
 
 
-def _chat_completion_response(content: str, model: str) -> dict[str, Any]:
+def _chat_completion_response(
+    content: str,
+    model: str,
+    executed_tools: list[dict[str, Any]] | None = None,
+) -> dict[str, Any]:
     return {
         "id": f"chatcmpl-{uuid.uuid4().hex[:12]}",
         "object": "chat.completion",
@@ -61,6 +65,9 @@ def _chat_completion_response(content: str, model: str) -> dict[str, Any]:
             }
         ],
         "usage": {"prompt_tokens": 0, "completion_tokens": 0, "total_tokens": 0},
+        # Cozy patch: expose the tools the agent invoked during this turn so
+        # downstream consumers (CozyEngineV2 Brain) can audit/observe routing.
+        "metadata": {"executed_tools": list(executed_tools or [])},
     }
 
 
@@ -303,6 +310,12 @@ async def handle_chat_completions(request: web.Request) -> web.Response:
     # -- non-streaming path (original logic) --
     _FALLBACK = EMPTY_FINAL_RESPONSE_MESSAGE
 
+    # Cozy patch: clear per-turn tool log before invoking the agent.
+    try:
+        agent_loop._last_run_tools.clear()
+    except AttributeError:
+        pass
+
     try:
         async with session_lock:
             try:
@@ -344,7 +357,11 @@ async def handle_chat_completions(request: web.Request) -> web.Response:
         logger.exception("Unexpected API lock error for session {}", session_key)
         return _error_json(500, "Internal server error", err_type="server_error")
 
-    return web.json_response(_chat_completion_response(response_text, model_name))
+    # Cozy patch: snapshot executed tools (if agent exposes them) for metadata.
+    executed_tools = list(getattr(agent_loop, "_last_run_tools", []) or [])
+    return web.json_response(
+        _chat_completion_response(response_text, model_name, executed_tools=executed_tools)
+    )
 
 
 async def handle_models(request: web.Request) -> web.Response:
